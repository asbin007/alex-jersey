# alex-jersey

Alex Jersey Shop — FIFA World Cup 2026 jersey e-commerce (Nepal). React + Vite frontend, Node.js API.

## Setup

```bash
# Client
cd client && npm install && npm run dev

# Server
cd server && npm install && cp .env.example .env && npm run dev
```
