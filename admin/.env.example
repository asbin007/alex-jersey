# No environment variables required.
# The admin panel talks directly to the backend at http://localhost:5000/api
# If you deploy to a different URL, set this:
# NEXT_PUBLIC_API_URL=https://your-api-domain.com/api
